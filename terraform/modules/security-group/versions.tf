# ----------------------------------------
# Terraform Version Constraint
# ----------------------------------------
# 共有モジュールは上限を切らない（利用側が CLI を上げられなくなるため）
terraform {
  required_version = ">= 1.5.0"
}
